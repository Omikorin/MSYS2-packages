# Maintainer: LoveSy <shana@zju.edu.cn>

pkgname=yodl
pkgver=4.05.01
pkgrel=1
pkgdesc='Implements a pre-document language and tools to process it'
arch=('i686' 'x86_64')
url='https://fbb-git.gitlab.io/yodl/'
msys2_repository_url='https://gitlab.com/fbb-git/yodl'
msys2_changelog_url='https://gitlab.com/fbb-git/yodl/-/blob/master/yodl/changelog'
msys2_references=(
  'anitya: 17582'
  'archlinux: yodl'
  "cpe: cpe:/a:yodl_project:yodl"
)
license=('spdx:AGPL-3.0-or-later')
depends=('bash')
makedepends=('icmake>=8.00.00' 'gcc')
source=("https://gitlab.com/fbb-git/${pkgname}/-/archive/${pkgver}/${pkgname}-${pkgver}.tar.gz")
sha512sums=('92f9eff80996d81dbc52f4dfe75e4f485d3e23e25f5d1fb64444ca706abe8d34fed480533b2c7bb0a91898e86636dde099a86447630b9d77e2d56965a17ab39a')

# prepare() {
# }

build() {
  cd "${srcdir}/${pkgname}-${pkgver}/${pkgname}"
  rm -f verbinsert/VERSION
  cp VERSION verbinsert/
  
  CXXFLAGS+=' --std=c++20'
  export CXXFLAGS
  ./build programs
  ./build macros
  ./build man
  ./build html
}

package() {
  cd "${srcdir}/${pkgname}-${pkgver}/${pkgname}"
  ./build install programs "${pkgdir}"
  ./build install macros "${pkgdir}"
  ./build install man "${pkgdir}"
  ./build install manual "${pkgdir}"
}
